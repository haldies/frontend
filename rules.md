Rules/Conditions:

The detection feature should follow the existing Context7 guidelines. You need to make sure that you understand how Context7 handles the context and flow.

The integration with Chrome DevTools should be clear. Ensure you are familiar with how Chrome DevTools' environment interacts with the web development stack to enable debugging and inspection.

Use RepoMix to analyze and read the structure of the repository files. RepoMix will help determine the correct locations and ensure that new keywords are properly placed within the configuration files.2